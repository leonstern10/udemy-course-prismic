import React from 'react';
import { RichText } from 'prismic-reactjs';
import styled from 'styled-components';

const ComparisonBox = (popularTag, feature1, feature2, feature3, price, readMore, serviceDescription, serviceLogo, webLabel, webLink ) => {
return(
    <h1>
        ho
    </h1>
)
}

export default ComparisonBox;